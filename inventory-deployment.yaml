apiVersion: apps/v1
kind: Deployment
metadata:
  name: inventory-service
  labels:
    app: inventory-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: inventory-service
  template:
    metadata:
      labels:
        app: inventory-service
    spec:
      containers:
        - name: inventory-service
          image: saihu-acr-registry-vpc.cn-hongkong.cr.aliyuncs.com/scm-prod/inventory-service:latest
          imagePullPolicy: Always
          ports:
            - containerPort: 4084
            - containerPort: 5084
          envFrom:
            - secretRef:
                name: inventory-service-secret
